version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Hello from CircleCI
          command: echo "CircleCI integration is working!"

workflows:
  ci:
    jobs:
      - build
